/**
 * Represents the two possible colors for pieces and cells in the game.
 * Used to distinguish between players (White and Black) and to define
 * the checkerboard pattern on the {@link Board}.
 */
public enum Color {
    /** Represents the Black player or a Black cell. */
    BLACK,
    /** Represents the White player or a White cell. */
    WHITE
}
